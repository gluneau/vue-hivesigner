{
  "name": "vue-hivesigner",
  "version": "0.5.0",
  "description": "HiveSigner plugin for Vue.js",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets babel-preset-env --out-dir dist",
    "prepare": "npm run build"
  },
  "keywords": [
    "hivesigner",
    "sc2",
    "vue",
    "plugin"
  ],
  "author": "mkt <kontakt@markus-kottlaender.de>",
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/mktcode/vue-hivesigner"
  },
  "dependencies": {
    "hivesigner": "^3.1.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
